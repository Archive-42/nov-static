extends profile
include includes/utils

block userTabContent
  ul.profileFeed
    h2.tabTitle #{user.firstName} #{user.lastName}'s Likes
    each like in userLikes
      li.card: +storyCardFull(like.Story)